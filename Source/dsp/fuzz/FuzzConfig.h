#pragma once

#include "FuzzType.h"

// FuzzConfig.h is an alias for FuzzType.h — both expose the FuzzConfig namespace.
// Include this header for FuzzConfig::mapDrive() and FuzzConfig::outputCompensation.
