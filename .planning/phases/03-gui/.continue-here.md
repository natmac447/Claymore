---
phase: 03-gui
task: 10-point physicality overhaul
total_tasks: 10
status: partially_complete
last_updated: 2026-02-24T10:45:00.000Z
---

<current_state>
Implementing 10 design changes from aggressive cairn-design-review comparing Claymore to 7 reference images (Dieter Rams Braun knobs, CNC knurled knobs, recorder app, dark slider UI, etc). Goal: transform flat illustration into convincing physical device.
</current_state>

<completed_work>
DONE in ClaymoreTheme.cpp:
- #1 Cap + skirt on knobs: drawRotarySlider now has separate skirt (darker sidewall band) and cap (top face). Skirt gradient darker than cap. Bright hairline lip at cap edge.
- #2 Heavy shadows: 5-pass shadow, peak alpha 25%, offset 3-4px, spread 4-6px
- #5 Lighter cutout: Changed from 0xff0e0e0e to 0xff2a2520 (warm dark gray). Stronger inner shadow gradient.
- #7 Indicator groove: Shadow line + highlight line flanking the main indicator
- #8 Knurled edge: Radial tick marks on skirt for knobs where radius > 18px. Alternating brightness.
- #10 Silk-screened labels: drawLabel now draws 1px shadow offset below text

NOT YET DONE — grain texture change was not applied yet.
</completed_work>

<remaining_work>
All remaining changes are in TWO files. Code snippets provided for direct implementation.

## ClaymoreTheme.cpp — 1 change remaining

### #3 Grain texture opacity (generateGrainTexture)
Change line in generateGrainTexture:
```cpp
// OLD:
juce::Colour (v, v, v).withAlpha (opacity * 0.025f)
// NEW:
juce::Colour (v, v, v).withAlpha (opacity * 0.07f)
```

## PluginEditor.cpp — 4 changes remaining

### #3 (continued) Apply grain to ALL zones
In paint(), after filling background and utility zone, apply grain to full bounds instead of just primaryZone:
```cpp
// Replace the current grain-on-primaryZone-only approach with:
const auto& grain = theme.getGrainTexture();
if (grain.isValid())
{
    g.setTiledImageFill (grain, 0, 0, 1.0f);
    g.fillRect (getLocalBounds());
}
```
Remove the separate grain fill on utilityZone (it's now covered by the full-bounds fill).

### #4 Beveled zone separators
Replace the 3 single-line separators (section 4 in paint) with bevel pairs:
```cpp
// 4. Zone separators — beveled edges for machined panel feel
auto drawBevel = [&](int xPos, int yPos, int w)
{
    g.setColour (juce::Colour (0x18000000));
    g.fillRect (xPos, yPos, w, 1);
    g.setColour (juce::Colour (0x10ffffff));
    g.fillRect (xPos, yPos + 1, w, 1);
};

drawBevel (headerZone.getX(), headerZone.getBottom(), headerZone.getWidth());
drawBevel (utilityZone.getX(), utilityZone.getY(), utilityZone.getWidth());
drawBevel (utilityZone.getX(), utilityZone.getBottom(), utilityZone.getWidth());
```

### #6 Embossed title
In section 5 of paint(), draw the title with deboss shadow+highlight before the main text:
```cpp
// Deboss shadow
g.setColour (juce::Colour (0x18000000));
g.drawText (title, textArea.translated (0, 1).toFloat(), juce::Justification::centredLeft, false);

// Deboss highlight
g.setColour (juce::Colour (0x0cffffff));
g.drawText (title, textArea.toFloat().translated (0.0f, -0.5f), juce::Justification::centredLeft, false);

// Main text (existing code)
g.setColour (juce::Colour (ClaymoreColors::primaryText));
g.drawText (title, textArea, juce::Justification::centredLeft, false);
```
Note: The drawText with float rect needs `textArea.toFloat()`. The shadow/highlight use translated rects.

### #9 Panel enclosure edge
Add at the very end of paint(), after version text (section 9):
```cpp
// 10. Panel enclosure edge — beveled metal border
{
    auto fullBounds = getLocalBounds().toFloat();
    g.setColour (juce::Colour (0x30000000));
    g.drawRect (fullBounds, 1.0f);
    g.setColour (juce::Colour (0x10ffffff));
    g.drawRect (fullBounds.reduced (1.0f), 1.0f);
}
```
</remaining_work>

<build_status>
NOT YET BUILT. The drawRotarySlider rewrite and drawLabel silk-screen changes are saved to disk but not compiled. The grain and PluginEditor changes have NOT been applied to files yet.
</build_status>

<next_action>
1. Apply grain opacity change in ClaymoreTheme.cpp generateGrainTexture (0.025f -> 0.07f)
2. Apply all 4 PluginEditor.cpp changes (#3 grain full bounds, #4 bevels, #6 embossed title, #9 panel edge)
3. Build and verify
4. Test in DAW and iterate
</next_action>
</content>
